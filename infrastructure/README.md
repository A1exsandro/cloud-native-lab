# Infrastructure

This directory contains the infrastructure definition for the Cloud Native Lab.

Infrastructure provisioning follows the Infrastructure as Code (IaC) approach using Terraform and Ansible.

## Structure

- terraform/ — Oracle Cloud infrastructure provisioning
- ansible/ — Operating system configuration and server automation

The infrastructure layer is responsible for preparing the environment before the Kubernetes platform is installed.